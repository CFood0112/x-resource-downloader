{
  "name": "x-resource-downloader",
  "version": "0.2.1",
  "private": true,
  "description": "X resource downloader for likes and bookmarks",
  "scripts": {
    "start": "node gui_server.js",
    "test": "node --test"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
